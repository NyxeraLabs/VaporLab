version: "3.9"

services:
  vaporlab-api:
    image: alpine:latest
    container_name: vaporlab-api-qa
    command: sh -c "while true; do sleep 3600; done"
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=quality-assurance
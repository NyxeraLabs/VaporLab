# Users Service Manual

Endpoints: `/users`, `/users/{id}`, `/users/rate-limit-bypass`.

Covers CRUD, BOLA/IDOR behavior, mass assignment, internal data exposure, and tenant boundary enforcement in secure mode.

# Admin Service Manual

Endpoints: `/admin/promote`, `/admin/tenant`, `/admin/debug`, `/chain/run`.

Covers function-level authorization flaws and chain orchestration across users, billing, and AI surfaces.
